[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]
cacheDirectories = ["node_modules"]

[phases.build]
cmds = [
  "npx prisma generate --schema=prisma/schema.prisma",
  "npm run build"
]

[start]
cmd = "npx prisma db push --schema=prisma/schema.prisma --accept-data-loss && node dist/server.js"
